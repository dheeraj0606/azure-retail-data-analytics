# File: config/env.yaml

environment: dev

storage:
  lakehouse_path: /lakehouse/default/

database:
  schema: dbo
  dim_customer: DimCustomer

scd:
  type: 2
  partition_columns:
    - EffectiveDate
    - IsCurrent
